import { X } from "lucide-react";
import { GenerateActionButton } from "../columns";

export const ActionButtonX = GenerateActionButton({
  title: "Quitar",
  icon: X,
  className: "text-red-500 hover:text-red-900"
})